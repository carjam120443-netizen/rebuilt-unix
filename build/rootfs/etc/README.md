# Rebuilt Unix Root Filesystem

Files placed under this tree are copied into the generated target filesystem.

Planned configuration areas include:

- `etc/rc.conf.d/` — Rebuilt Unix service configuration
- `etc/ssh/` — SSH configuration, when enabled
- `etc/pkg/` — package configuration
- `etc/motd` — login message
- `etc/profile.d/` — shell environment additions
