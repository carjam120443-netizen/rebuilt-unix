# Rebuilt Unix temporary-root workspace
TEMPROOT_BASE="/temproot"
TEMPROOT_SCOPE="terminal"
TEMPROOT_CLEAN_ON_REBOOT="yes"
TEMPROOT_CLEAN_ON_NEW_TERMINAL="yes"
TEMPROOT_SUDO_CHDIR="yes"

# Important: this is a temporary working directory, not a security sandbox.
# Commands using absolute paths can still modify the real filesystem when run via sudo.
