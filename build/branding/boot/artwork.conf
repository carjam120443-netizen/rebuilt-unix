# Rebuilt Unix boot artwork configuration

# Primary artwork used by the default boot theme.
BOOT_ARTWORK="rebuilt-unix.png"

# Optional themed artwork. These remain disabled until compatible artwork
# is supplied and its licensing/trademark requirements have been checked.
FREEBSD_TUX_ARTWORK="freebsd-tux.png"
OPENBSD_TUX_ARTWORK="openbsd-tux.png"
NETBSD_TUX_ARTWORK="netbsd-tux.png"

# Boot theme mode.
BOOT_THEME="rebuilt-unix"
