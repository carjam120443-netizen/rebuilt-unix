# Rebuilt Unix Package Configuration

Package manifests live here. The target system uses FreeBSD `pkg`, not `apt`.

The initial required package set is:

- `pkg`
- `sudo`

Additional packages can be separated into live-environment, installer, desktop, and optional package lists as the project grows.
